# Ollama Environment Template

# Version (use 'latest' or specific tag like '0.5.1')
OLLAMA_VERSION=latest

# NVIDIA GPU (auto-detected when nvidia-container-toolkit installed)
# DOCKER_NVIDIA_VISIBLE_DEVICES=all

# AMD GPU (uncomment for AMD cards with ROCm support)
# HSA_OVERRIDE_GFX_VERSION=10.3.0
