{
  "name": "ozma-renderer",
  "version": "1.0.0",
  "description": "Screen rendering service for ozma — generates images for any display",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.44",
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "sharp": "^0.33.0"
  },
  "engines": {
    "node": ">=18"
  }
}
