[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: fast unit test, no external dependencies
    e2e: end-to-end integration test requiring infrastructure
    security: security and compliance test
    slow: test taking more than 5 seconds
addopts = --strict-markers
filterwarnings =
    ignore::DeprecationWarning
