# Ozma Controller — Python dependencies
# Install: uv pip install -r controller/requirements.txt

# Core framework
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0

# Networking / discovery
zeroconf>=0.132
aiohttp>=3.8

# Input capture
evdev>=1.7

# VNC streaming
asyncvnc>=1.3
numpy>=1.24
pillow>=10.0

# Audio (MIDI controllers — optional, install python-rtmidi separately if needed)
mido>=1.3

# WebSocket clients
websockets>=12.0

# Cryptography (Ed25519 identity keys, AEAD encryption)
pynacl>=1.5

# HTTP client (MSP dashboard health checks, WireGuard peering API)
httpx>=0.27

# YAML config (optional — for controls.yaml)
pyyaml>=6.0

# GraphQL
strawberry-graphql>=0.237
strawberry-graphql[fastapi]>=0.237
