import { Navigate } from 'react-router-dom'

export const routerConfig = {
  paths: {
    nodes: '/nodes',
    node: '/nodes/:id',
    controls: '/controls',
    settings: '/settings',
  },
}

export { Navigate }
